<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.email, user %>
  <div> asdasd asdd </div>
  <% if true #current_user.admin? && !current_user?(user) %>
      | <%= link_to "delete", user, method: :delete,
                    data: { confirm: "You sure?" } %>
  <% end %>
</li>
